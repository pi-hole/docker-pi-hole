name: Build Image and Publish
on:
  workflow_dispatch:

permissions:
  contents: read

env:
  components_branch: development
  ftl_branch: new/dot-doh-client

jobs:
  build-prepare:
    runs-on: ubuntu-24.04
    outputs:
      components_branch: ${{ env.components_branch }}
      ftl_branch: ${{ env.ftl_branch }}
    steps:
      # FIXME: can't use env object in reusable workflow inputs: https://github.com/orgs/community/discussions/26671
      - run: echo "Exposing env vars for reusable workflow"

  build:
    uses: docker/github-builder/.github/workflows/build.yml@5f637c833aa76bc99372a1dc9a6f8bcd8056fb85 #v1.12.0
    needs:
      - build-prepare
    permissions:
      contents: read # same as global permissions
      id-token: write # for signing attestation(s) with GitHub OIDC Token
      packages: write # required to push to GHCR
    with:
      setup-qemu: true
      cache: true
      cache-scope: build
      cache-mode: max
      fail-fast: true
      context: src
      output: image
      build-args: |
        PIHOLE_DOCKER_TAG={{meta.version}}
        FTL_BRANCH=${{ needs.build-prepare.outputs.ftl_branch }}
        CORE_BRANCH=${{ needs.build-prepare.outputs.components_branch }}
        WEB_BRANCH=${{ needs.build-prepare.outputs.components_branch }}
        PADD_BRANCH=${{ needs.build-prepare.outputs.components_branch }}
      platforms: linux/amd64,linux/386,linux/arm/v6,linux/arm/v7,linux/arm64,linux/riscv64
      push: true
      set-meta-labels: true
      meta-images: |
        pihole/pihole
        ghcr.io/${{ github.repository_owner }}/pihole
      meta-tags: |
        type=raw,value=dot-doh-client-test
      meta-flavor: |
        latest=false
      # FIXME: GHCR does not support the referrers API and spams the registry with sha-tagged images when cosigned: https://github.com/docker/github-builder/issues/109
      sign: false
    secrets:
      registry-auths: |
        - registry: docker.io
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_PASS }}
        - registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
