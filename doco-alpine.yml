alpine:
  build: .
  dockerfile: alpine.docker
  environment:
    # Your docker host IP is required
    - piholeIP: 192.168.1.102
  ports:
    - "53:53/tcp"
    - "53:53/udp"
    - "80:80/tcp"
  cap_add:
    - NET_ADMIN
